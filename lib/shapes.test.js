// import the required files - shapes.js - we need the shape classes
// test suit
    // has 3 initial test, 6 in total
        // each shape needs a test - triangle, circle, square
            // const shape = new Triangle();
                // shape.setColor("blue");
                // expect(shape.render)